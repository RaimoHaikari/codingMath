const initialState = {
    on: false,
    str: undefined
}


const trigReducer = (state = initialState, action) => {

    switch(action.type) {
            
        default:
            return state;
    }
}

export default trigReducer;
